
"""WHAT IS CSV?: the acronym stands for coma separated values, each
row in this file is a record. Each record is made up of at least two fields
or columns separated by a coma hence the name CSV.

this exercise includes
1. reading a csv file from a directory using the csv.reader()
2. reading a csv file from cwd using a dict reader even though the data is
not dictionary formatted
3. writing csv using csv.writer()
4. writing a csv file using a DictWriter()"""






